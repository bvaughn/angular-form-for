@require '_variables';

field-label {
  display: inline-block;
}

.help-icon-stack {
  position: relative;
  font-size: 10px;

  .help-background-icon,
  .help-foreground-icon {
    text-align: center;
  }

  .help-background-icon {
    color: $color-fill-5;
  }
  .help-foreground-icon {
    color: white;
    position: absolute;
    left: 0;
  }
}

.field-label {
  position: relative;
  cursor: inherit;
  font-weight: normal;

  &:hover {
    .help-icon-stack {
      .help-background-icon {
        color: $color-fill-5-hover;
      }
    }
  }
}

.field-label-required-label {
  color: $color-text-1;
  font-style: italic;
  font-size: .8em;
}

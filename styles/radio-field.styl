@require '_mixins';
@require '_variables';

radio-field {
  display: block;
  position: relative;
}

.form-for-field {
  > input[type="radio"] {
    @extend $form-for-hidden-input

    +label {
      cursor: pointer;
      display: inline-block;

      &:before {
        @extend $form-for-toggle-ui

        border-radius: 10px;
        content: " "; // Hidden
        font-size: 11px;
      }
    }

    &:focus + label:before {
      border-color: $border-color-focused;
      box-shadow: $box-shadow-focused;
    }

    &:checked + label:before {
      content: "\f111";
    }

    &:disabled {
      opacity: 0;
      cursor: default;
      pointer-events: none;

      +label {
        cursor: default;
        opacity: $opacity-disabled;

        &:before {
          cursor: default;
        }
      }
    }
  }

  > input[type="radio"]:invalid,
  &.invalid > input[type="radio"] {
    +label:before {
      border-color: red;
    }

    &:focus + label:before {
      box-shadow: $box-shadow-error-focused;
    }
  }
}
